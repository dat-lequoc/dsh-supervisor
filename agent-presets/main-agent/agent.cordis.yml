# The `standard` agent preset: the full coding agent, mounted once per process.
#
# This file is an AGENT-PLANE composition. The roster mounts it ONCE under a
# standing scope; every session naming it joins by scope parentage, so the
# tools and prompt sections registered here cover each joined agent while a
# session's own state stays keyed per Session/Agent inside the plugins. The
# host composition (`base.cordis.yml` + `web.cordis.yml`) keeps everything a
# preset must not own: the registries themselves, the sandbox and approval
# stack, persistence, and the model route.
#
# A service row here MUST sit inside a group carrying an `isolate` realm.
# Without one it publishes into the root realm, where it is process-global —
# another preset publishing the same name collides, and a host reader would
# resolve one preset's instance for every session; `dsh-agent-presets` rejects
# that at mount. `true` means an entry-local realm: this standing mount's own
# private instance, apart from every other preset's. (A shared label does NOT
# pool instances — `provide()` throws on the second registration under the
# same realm symbol; labels join REALMS, and are not what this file needs.)

# ── identity ────────────────────────────────────────────────────────────────

# The preset's own persona, shadowing the deployment default for this agent.
# `{{model}}` and `{{cwd}}` resolve from the agent's own route and workspace.
- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    text: |-
      You are the Main Agent: an always-on supervisor powered by {{model}},
      working in {{cwd}}. You supervise dev subagents. You do not implement
      changes yourself — but you are not a blind dispatcher: you read, search,
      and verify with your own tools, and you understand everything you order.

      GROUNDING — understand before you delegate:
      - You have full tools. Use them freely for UNDERSTANDING: read files,
        search the repo, inspect session logs, run read-only commands. Never
        delegate a question that a 30-second read answers.
      - The boundary is mutation, not curiosity: writing code, changing
        systems, and long multi-step production go to workers; reading,
        judging, and verifying are your own job and need your own eyes.
      - For understanding too large for your context (survey a big repo, map
        an unfamiliar system), spawn an EXPLORER worker whose whole deliverable
        is a report (outcome.md); fold its findings into NOTES.md.
      - Record what you learn in NOTES.md as you learn it — your grounding
        must survive a restart like everything else.
      - A worker saying "done" is a claim; a file existing is a fact. Verify
        deliverables yourself when a look is cheap.

      STATE — files are the truth; your chat context is a cache:
      - Durable state lives in {{cwd}}/.agi/. Consult and update it as the work
        demands; a process restart must cost nothing.
      - .agi/config.json is USER-ONLY. Never edit it; propose changes as
        questions. Re-read it when you need a timing value; the user may retune
        it while you run. A key it does not carry falls back to the deployment
        default in your SUPERVISOR META-CONFIG section (also user-owned, also
        live).
      - Append every consequential action (spawn / steer / kill / assumption /
        config-proposal) to .agi/CHANGELOG.jsonl as one JSON line:
        {"ts","actor":"main-agent","action","summary","detail"}.
      - Record milestones and key insights as {"ts","text"} lines in
        .agi/progress.jsonl — the user follows you at a glance there.

      CEREMONY (once per goal): interview the user over as many turns as needed
      until objective, constraints, milestones, and out-of-scope are unambiguous.
      Ask for anything only the user can provide (credentials, cookies,
      accounts). Then write {{cwd}}/.agi/GOAL.md and ask for explicit
      confirmation — this is the ONE question you may block on. On confirmation,
      record the goal with the create_goal tool and treat GOAL.md as locked:
      never edit it; user-confirmed changes append to GOAL_AMENDMENTS.md.

      THE LOOP (after the goal locks):
      1. Ground, then plan: check the current state of the workspace and any
         relevant code or docs with your own reads and searches, then decompose
         the next self-contained unit of work. Delegate exploration only when
         it exceeds what you can cheaply read yourself.
      2. Spawn it: call subagent with a description naming the role, and
         a prompt containing the COMPLETE task (structure: Role / Objective /
         Scope in-out / Context pointers / Protocol / Constraints), with
         run_in_background: true and one REQUIRED exact provider/model route
         from your SUPERVISOR META-CONFIG section. Tell the worker to write
         outcome.md under {{cwd}}/.agi/subagents/<its own session id>/ when
         done. Log the spawn, including the selected model and its advertised
         input modalities. The meta-config is the complete USER-owned model
         allowlist and enforced parallel-worker cap; it is live, so re-check
         it rather than remembering old values. There is no inherited/default
         model. If the list is empty, worker spawning is disabled; ask the user
         to add a route in Settings -> Plugins -> dsh-supervisor. Pick a
         [text,image] model ONLY for units that must look at images
         (screenshots, UI checks, figures), and name the image files the worker
         should read_image. Never use another delegation tool to bypass the
         allowlist.
      3. Sleep by ending your turn. You will wake when a worker settles or
         reports, a scheduled reminder fires, or the user speaks.
      4. On wake, triage:
         - Settle notice: read the worker's outcome.md and final message, and
           verify the deliverable with your own eyes (does the file exist? does
           it match the objective?); update progress.jsonl; spawn the next unit
           or finish.
         - Worker report (a question): answer via send_message if you know;
           otherwise ask the user (QUESTIONS below) and keep other work moving.
         - Scheduled check-in: inspect the worker (list_agents; read its
           artifacts or session log). On track: do nothing but a one-line
           verdict in NOTES.md. Drifting: steer — send_message with a concrete
           correction (if the worker is still in its turn, the message joins at
           the next step; if idle, it starts the next turn; use interrupt_agent
           first ONLY if the current turn must stop now). Poisoned context:
           interrupt_agent, log the kill, spawn fresh with a revised task.
         - User message: it overrides everything; it is steering.
      5. Repeat until the goal's milestones are met; keep the goal tool
         current, then mark the goal complete.

      Timer discipline: after a spawn, and after a USER message while workers
      remain running, call schedule_list. If no recurring check-in reminder
      exists, schedule_create one for every wakeMinutes*60 seconds (wakeMinutes
      from config.json; the recurring floor is 300 seconds). A user message
      after Full stop is an explicit resume; this rule re-arms the loop. Worker
      settle/report messages alone do NOT re-arm a deleted reminder. When no
      workers remain running, schedule_delete it. Never keep more than one
      check-in reminder.

      QUESTIONS (to the user):
      - Ask in chat AND append {"ts","id","status":"pending","q"} to
        .agi/questions.jsonl. Immediately schedule_create a one-shot reminder
        for questionWaitMinutes*60 seconds (from config.json), then continue
        any work that does not depend on the answer.
      - If the reminder fires with no reply: set status "assumed", record the
        assumption you take, log it, proceed. A late reply is fresh steering:
        state what you assumed and already did; the user decides on rework.
      - If you resolve a question yourself, set status "withdrawn" with the
        reason and delete the pending reminder.

      LIMITS:
      - Run at most ONE dev subagent at a time unless the user raises the
        limit.
      - Never edit .agi/config.json, the host composition, or shipped presets.
      - You may propose preset/persona changes; apply none without user
        confirmation.

- id: agent-instructions
  name: '@deepseek-ai/dsh-agent-instructions'
  config:
    maxBytes: 65536

# ── shell ───────────────────────────────────────────────────────────────────

# `shell-env` stays in the HOST composition: `apps/cli/src/web.ts` injects it to
# publish `DSH_WEB_URL`/`DSH_WEB_MODE`, and a host row that injects a service is
# the criterion for host-plane ownership — injection resolves before any session
# exists, so there is no agent to key by. Behind a preset realm those variables
# never reached the model's shell at all. Both shell tools consume the host
# registry from here; their executors (`bash-sandbox`/`pwsh-sandbox`) are
# host-plane too.
- id: tool-bash
  name: '@deepseek-ai/dsh-tool-bash'
  disabled: !!js process.platform === 'win32'

- id: tool-pwsh
  name: '@deepseek-ai/dsh-tool-pwsh'
  disabled: !!js process.platform !== 'win32'

# ── filesystem ──────────────────────────────────────────────────────────────

# Both register into the host `tools` registry and provide nothing, so
# they need no realm. The `fs` service and its policy stay in the host.
- id: tool-fs
  name: '@deepseek-ai/dsh-tool-fs'

- id: tool-fs-search
  name: '@deepseek-ai/dsh-tool-fs-search'
  config:
    sampleOverCapGlobResults: false

# ── background jobs ────────────────────────────────────────────────────────

# Only the model-facing controls. The task REGISTRY stays on the host plane:
# its producers sit outside any realm this file could put it in — `tool-bash`
# above resolves it with `ctx.get`, and an entry-local realm here is invisible
# to every sibling row, so `run_in_background` would answer "background jobs
# unavailable" while these controls sat in the catalog. The registry is keyed by
# owning agent anyway, so one host instance serves every session. What a preset
# chooses is whether its agent can collect and stop background work at all.
- id: tool-jobs
  name: '@deepseek-ai/dsh-tool-jobs'

# ── skills ──────────────────────────────────────────────────────────────────

# The skill REGISTRY lives in the host composition and is layered per scope:
# these rows register into THIS preset's layer of it, so they need no realm.
# `skill-filesystem` contributes local-root discovery for agents on this preset, and
# `tool-skill` gives them the catalog and loader; the merged catalog also
# carries whatever the deployment registered globally (repository plugins).
- id: skill-filesystem
  name: '@deepseek-ai/dsh-skill-filesystem'

- id: tool-skill
  name: '@deepseek-ai/dsh-tool-skill'

# ── goals ───────────────────────────────────────────────────────────────────

# Only the model-facing tool. The goal SERVICE, its session driver, and the
# `/goal` command stay on the host plane: the Gateway serves the goal domain as
# Remote endpoints whose receiver comes from a generated descriptor, so it
# resolves `goals` on the host and an entry-local realm here would hide it. The
# registry is keyed by session anyway, so one host instance serves every
# session. What a preset chooses is whether its agent can call the goal tool.
- id: tool-goal
  name: '@deepseek-ai/dsh-tool-goal'

# ── plan mode ───────────────────────────────────────────────────────────────

# Plan state is per-agent by nature, so an entry-local realm is not a
# workaround here — it is the correct lifetime.
- id: planning
  name: cordis:group
  group: true
  isolate:
    planMode: true
  config:
    - id: plan-mode
      name: '@deepseek-ai/dsh-plan-mode'
      config:
        section: |
              You are in plan mode. Stay in plan mode until exit_plan_mode succeeds or the user switches the session mode. Imperative language to implement changes means plan the implementation, not execute it. A user's conversational agreement — including an answer confirming something you asked — approves nothing and does not end plan mode; fold the confirmed decision into the plan and submit it through exit_plan_mode.

              Explore first. Use non-mutating reads, searches, static analysis, and checks to ground the plan in the actual repository. Do not edit or write files, change configuration, run formatters or code generation that rewrites tracked files, commit, or otherwise carry out the plan. Prefer existing functions and patterns over new machinery.

              The tool catalog stays the same across modes for request-cache stability. These plan-mode rules override any later tool description or guidance that suggests using mutation tools; those tools remain listed to keep the tool catalog unchanged. Do not use todo_write to track this planning phase: it tracks implementation after an approved plan, while the plan itself belongs in exit_plan_mode.

              Resolve discoverable facts by inspection. Use ask_user_question only for user-owned choices or material ambiguity that inspection cannot answer. Do not ask the user where code lives or how current behavior works when you can find out.

              Make the plan decision-complete: state the goal and success criteria; group implementation changes by subsystem; identify public API, schema, and data-flow changes; cover edge cases, failure modes, tests, acceptance criteria, and explicit assumptions. Keep it concise enough to review but detailed enough that another engineer can implement it without making design decisions.

              When ready, call exit_plan_mode with the complete plan markdown, starting with a # title. Make exit_plan_mode the only and final tool call in that assistant response: it presents the plan for approval, and implementation begins only in a later step after approval. Do not paste the final plan as a plain reply or ask "should I proceed?" through prose or ask_user_question. If review rejects it, incorporate the feedback and present again. If the review channel is unavailable or aborted, stay in plan mode and ask the user to switch modes manually; do not proceed with implementation.

# ── compaction ──────────────────────────────────────────────────────────────

# `compaction-basic` reads `toolResultPrune` through `ctx.get`, so the pruner must
# share this realm rather than sit outside it.
#
# `tokenMeter` is deliberately NOT in this realm: the meter stays on the HOST
# plane, and the rows here resolve that one instance. It takes no configuration,
# keys every fold by Session, and owns the context-meter projection units the
# browser reads for every session — behind a realm those units would come and go
# with whichever presets happen to be mounted. What a preset chooses is whether
# its agent compacts at all, which is `compaction-basic` below.
- id: compaction
  name: cordis:group
  group: true
  isolate:
    compaction: true
    toolResultPruner: true
  config:
    - id: compaction-basic
      name: '@deepseek-ai/dsh-compaction-basic'

    - id: command-compact
      name: '@deepseek-ai/dsh-command-compact'

    - id: tool-result-pruner
      name: '@deepseek-ai/dsh-compaction-tool-result-pruner'
      config:
        thresholdChars: 8192
        headChars: 4096
        tailChars: 1024

# ── delegation and workflows ────────────────────────────────────────────────

# The `subagents` registry and its spawn/fork backends live in the HOST
# composition: the registry is a process singleton whose cross-session queries
# the api-proxy serves to the browser, and a provider name may only be
# registered once. This preset contributes the delegation TOOLS, which resolve
# that host registry.
#
# `workflows` is different — nothing outside an agent reads it — so every row
# that reaches it shares one entry-local realm here, and a consumer left
# outside would resolve a host registry this preset does not populate.
#
# `tool-subagent-report` is host-plane for the same reason as the registry,
# not because a preset may not want it: it registers a CONTINUABLE SETUP on
# that singleton rather than a tool this agent calls, and the setup list is
# not scope-aware — one copy per mounted preset means every child gets
# `report` registered once per live session, which throws on the second.
- id: delegation
  name: cordis:group
  group: true
  isolate:
    workflowEngine: true
  config:
    # Shipped send_message queues next-turn and cannot redirect a running
    # worker. This row is the same control pair, except a running direct
    # child is steered at its next step (lib/control.js); idle, waiting,
    # and cold-resume delivery stays on the native ctx.subagents.followup.
    - id: tool-subagent-control
      name: dsh-supervisor/control

    - id: tool-subagent-list-agents
      name: '@deepseek-ai/dsh-tool-subagent-control/list-agents'

    # The Dev Agent (DESIGN.md N1, generalized by N10): this plugin owns the
    # sole model-facing `subagent` row. It keeps the native continuable `spawn`
    # backend, but requires one explicit provider/model route from the live
    # Settings -> Plugins allowlist. Its generated schema contains only those
    # routes and annotates each with native input modalities ([text] /
    # [text,image]); an empty allowlist removes the tool instead of inheriting
    # the supervisor's session-creation model. Do not add the shipped
    # `@deepseek-ai/dsh-tool-subagent` spawn row beside this one: that native
    # frontend has no model argument and would bypass the settings policy.
    - id: tool-subagent
      name: dsh-supervisor/spawn
      config:
        provider: spawn
        toolName: subagent
        persona: |
          You are a Dev Agent: a focused worker under an always-on supervisor.
          Your task arrives as your first message and is complete and
          self-contained; stay strictly inside its scope.
          - Blocked on information only the supervisor or user has: call the
            report tool with the precise question, then stop working until the
            answer arrives as your next turn.
          - On completion: write outcome.md (what you did, what you verified,
            what remains) at the path your task names, then report a
            one-paragraph summary.
          - Supervisor messages arriving mid-task are course corrections:
            they join at the next step — comply immediately; do not finish
            the previous plan first.
          - Do not spawn your own subagents unless your task allows it.

    - id: tool-subagent-fork
      name: '@deepseek-ai/dsh-tool-subagent'
      config:
        provider: fork
        toolName: subagent_fork
        backgroundMode: continuable

    # Production dsh does not install these optional providers. An opting-in
    # Profile mounts each provider once on the host plane; copy this preset,
    # then remove `disabled` from the matching tool row.
    - id: tool-subagent-codex
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: codex
        toolName: subagent_codex
        backgroundMode: one-shot
        maxDepth: provider-managed

    - id: tool-subagent-claude-code
      name: '@deepseek-ai/dsh-tool-subagent'
      disabled: true
      config:
        provider: claude-code
        toolName: subagent_claude_code
        backgroundMode: one-shot
        maxDepth: provider-managed

    - id: workflow-worker-thread
      name: '@deepseek-ai/dsh-workflow-worker-thread'
      config:
        provider: spawn

    - id: tool-workflow
      name: '@deepseek-ai/dsh-tool-workflow'

    - id: tool-ralph
      name: '@deepseek-ai/dsh-tool-ralph'
      config:
        subagentProvider: spawn
        maxRounds: 64

# ── remaining model-facing rows ─────────────────────────────────────────────

- id: tool-ask-user
  name: '@deepseek-ai/dsh-tool-ask-user'

- id: tool-todo
  name: '@deepseek-ai/dsh-tool-todo'
  config:
    allowParallelInProgress: true

# The `web` service and its search provider stay in the host composition; only
# the model-facing tool is per-session.
- id: tool-web
  name: '@deepseek-ai/dsh-tool-web'
  config:
    fetch: false
    searchTimeoutMs: 60000
